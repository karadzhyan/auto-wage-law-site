# Release fidelity ledger

Reviewed 2026-07-18 against the three accepted concept images in this directory.

## Homepage

1. **Hierarchy:** Preserved the concept’s quiet header, oversized two-tone hero, role-first diagnostic, audience split, role navigator, exposure module, open library, developments, and dark footer in the approved order.
2. **Signature motif:** Implemented the diagnostic line and five role nodes as code-native HTML/CSS. The release version reduces the concept’s dense branching map to a legible role-to-issue ribbon, particularly on mobile.
3. **Typography and color:** Uses local Archivo, Source Sans 3, and IBM Plex Mono with the approved white/ink/cobalt/mist palette. Coral appears only for a potential shortfall.
4. **Content density:** Converted repeated dashboard cards into bordered sheets, rows, lists, and aligned rails. The exposure preview keeps its illustrative inputs and exclusions visible beside the number.
5. **Responsive behavior:** The two-column sections become a single, ordered reading flow at 390px without changing the information hierarchy or producing horizontal overflow.

## Pay Plan Check

1. Preserved the desktop worksheet/findings split and stacked it in that order on mobile.
2. Preserved the approved 46-hour, 38-hour, 8-hour illustrative fixture.
3. Replaced the concept’s compliance-sounding verdict with a bounded “potential time gap” finding and explicit scope language.
4. Kept fields, reasons, record pull-list, reset, and print actions visible in one sheet.
5. Omitted account/save-progress UI because inputs intentionally stay browser-local and are not persisted.

## Flagship article

1. Preserved the large editorial title, annotated workday, reading column, desktop table-of-contents rail, review stamp, authority panel, and related-reading rows.
2. Rebuilt the workday graphic as semantic HTML/CSS so it remains printable, responsive, and editable.
3. Consolidated primary-source links into one authority panel to avoid repeating the same citations.
4. Maintained a 46rem reading measure with full-width evidence modules.
5. Removed the desktop rail on narrow screens while retaining every article section in document order.

## Above-the-fold copy diff

All five locked strings match the approved specification exactly:

- Brand: `Auto Wage Law`
- Heading: `Dealership pay, measured against the law.`
- Support: `Independent guidance for the people who run dealerships and the people who work in them.`
- Primary action: `Find your pay plan`
- Secondary action: `Run a self-audit`
